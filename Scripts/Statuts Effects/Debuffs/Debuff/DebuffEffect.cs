using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DebuffEffect : Effect
{

    public override void ApplyEffect()
    {
        //can call other methods from within here if there is anything extra/special that should happen
    }

    protected override void RenewEffect()
    {
        //can call other methods from within here if there is anything extra/special that should happen
    }

}
